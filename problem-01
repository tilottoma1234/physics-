#include<stdio.h>
#include<stdio.h>
int main(){
double lambda,theta,d;
double m;
printf("Enter wavwlength (nm):");
scanf("%lf",&lambda);
if (lambda<380||lambda>750){
printf("out of the range.please enter a valid number.\n");
return 0;
}
printf ("Enter angel theta (in degrees):");
scanf("%lf",&theta);
printf("Enter slit seperation d(in nm):");
scanf("%lf",&d);
double theta_radian =theta *(M-PI /180.0);
m = (d8sin(theta_radian))/lambda;
int m_order =(int)round(m);
printf("%d-th order maxima\n",m_order);
return o;
}
